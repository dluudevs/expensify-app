node_modules/
public/dist

# ignore the 4 asset files in dist as they get generated when build is run. if they're uploaded there would be conflicts depending on which build you're using (dev or prod)    